"use strict";
let animals = ['lion', 'tiger', 'cheetah', 'leopard', 'jaguar', 'lynx'];
animals.forEach(animal => {
    console.log(`The ${animal} is a big cat.`);
});
/*// Define an array of animals
const animals: string[] = ['lion', 'tiger', 'cheetah', 'leopard', 'jaguar', 'lynx'];

// Define a function to highlight animals with a common trait
function highlightAnimalsWithTrait(animals: string[], trait: string): void {
    for (let i = 0; i < animals.length; i++) {
        console.log(`The ${animals[i]} is a ${trait}.`);
    }
}

// Call the function to highlight animals with a common trait
highlightAnimalsWithTrait(animals, 'big cat');*/
